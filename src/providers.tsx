// This file intentionally left blank to satisfy Vite if imported.
// In a real app we would set up actual providers here.
import React from 'react';
export const AppProvider = ({ children }: { children: React.ReactNode }) => <>{children}</>;
